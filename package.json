{
  "name": "project-boost",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "npm install",
    "build": "concurrently \"cd server && npm i\" \"cd client && npm i\"",
    "start": "npm run prebuild && concurrently \"cd server && npm start\" \"cd client && npm start\"",
    "startLocal": "concurrently \"cd server && npm start\" \"cd client && npm start\""
  },
  "dependencies": {
    "concurrently": "^5.1.0"
  }
}
