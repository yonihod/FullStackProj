{
  "name": "project-boost",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && concurrently \"cd server && npm i\" \"cd client && npm i\"",
    "start": "npm run build && concurrently \"cd server && npm start\" \"cd client && npm start\"",
    "startLocal": "concurrently \"cd server && npm start\" \"cd client && npm start\""
  },
  "dependencies": {
    "concurrently": "^5.1.0"
  }
}
